import json

input_dict = url(data/languages.json)
result = []

for key, value in json_dict.items():
    # Append a new dictionaty separating keys and values from the original dictionary to the array:
    json_array.append({'key': key, 'value': value})
